# 작업 진행상황 (2026-02-28)

## 진행 내용
- 요구사항 반영: **로그인 검증 없이(구글 docs 스타일)** 협업 가능한 익명 식별 방식으로 전환했습니다.
- IP 기반 actor 발급을 제거하고, 서버가 최초 접속 시 **익명 세션 쿠키(`dd_actor`)**를 발급하도록 변경했습니다.
  - 포맷: `anon-xxxxxxxxxxxx`
  - `httpOnly`, `sameSite=lax`, `maxAge=30일`
- 클라이언트는 기존처럼 `/api/realtime/actor`를 호출하지만, 이제 서버가 쿠키를 기준으로 같은 브라우저에 동일 actor를 반환합니다.
- 결과적으로 URL/localStorage/IP에 의존하지 않고도 로그인 없이 "이 사용자가 최근 조작자"를 추적할 수 있습니다.

## Next action item
1. 룸 단위 표시명(예: `user1`, `user2`)을 actorId 매핑 테이블로 관리해 UI 가독성을 높입니다.
2. 접속자 목록/활성 사용자 표시를 위해 Realtime Presence를 연동합니다.
3. actor 쿠키 만료/재발급 UX(예: 30일 경과 시 새 ID 안내)를 정리합니다.

## 주의사항
- 로그인 없는 익명 협업 방식이므로, **신원 보장**은 제공하지 않습니다.
- 쿠키 삭제/브라우저 변경 시 actor가 바뀔 수 있습니다.
- 현재 구조는 협업 식별/충돌 완화 목적이며, 강한 권한 통제가 필요하면 Auth+RLS가 필요합니다.

## 사용자가 해야할 일
1. 같은 브라우저 재접속 시 `최근 조작자`가 일관되게 유지되는지 확인해주세요.
2. 다른 브라우저/시크릿 모드에서 서로 다른 actor로 보이는지 확인해주세요.
3. UI에 표시할 익명 이름 정책(`anon-xxxx` 그대로 vs `user1/user2` 별칭)을 결정해주세요.
