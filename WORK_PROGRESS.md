# 작업 진행상황 (2026-08-06)

## 진행 내용

### 1. 방 참여/생성 UX (`components/realtime/RoomSharePanel.tsx`)
- 방 만들기: 진행 중 버튼 비활성 + `방 만드는 중...` 표시, 실패 시 에러 배너 노출.
- 방 참여: 이제 이동 전에 실제로 방을 확인합니다.
  - 방 ID 형식 검증(UUID) + **공유 링크를 통째로 붙여넣어도** roomId를 추출.
  - 없는 방 / 다른 게임의 방 / 만료·종료된 방이면 각각 다른 안내 문구로 실패.
- 접속자 목록을 별칭 칩(`유저1`, `유저2`...)으로 표시하고, `최근 조작`도 raw actorId 대신 별칭으로 표기.

### 2. 익명 식별 UX (`lib/realtime/actorAlias.ts`, `lib/realtime/clientActor.ts`)
- actorId 정렬 순서로 `유저N` 별칭을 만드는 유틸 추가. 같은 참가자 집합을 보는 클라이언트끼리 별칭이 일치합니다.
- `dd_actor` 쿠키가 만료/삭제되어 새 actorId가 발급되면 감지해, 방 패널에서 **한 번만** 안내 문구를 노출합니다.

### 3. 폭탄 돌리기 DoD 보완 (`app/hot-potato/page.tsx`)
- **종료 판정 단일화**: 타이머 만료/인원 부족 종료를 Host만 수행. Host가 나간 경우 접속자 중 actorId가 가장 앞서는 사람이 대신 판정합니다. (중복 종료·중복 히스토리 방지)
- **종료 우선 원칙**: 타이머가 지난 뒤 들어온 PASS는 소유자를 바꾸지 않고 그대로 종료로 확정합니다.
- **Ready 반영**: 참가자가 모두 준비돼야 시작 가능(`모두 준비되면 시작할 수 있습니다.`).
- **진행자 설정 UI**: 라운드 시간(15/30/60초), `진행자만 다음 사람 누르기` 토글. 진행 중에는 변경 불가.
- **지난 라운드 히스토리** 표시(라운드 번호 / 당첨자 / 종료 사유).
- 만료되거나 없는 방 링크로 들어오면 에러 배너로 안내.

### 4. 보안/운영 (`lib/realtime/rooms.ts`, `docs/supabase-rls-guide.md`)
- 입력값 검증: 방 이름(제어문자 제거·30자 제한), `max_capacity`(2~20), `game_type` 화이트리스트.
- 방 만료: `ROOM_TTL_MS = 24시간`. 만료된 방은 참여/로드 단계에서 차단.
- 방 생성 rate limit(클라이언트): 10분에 5개까지.
- 에러 정리: `RealtimeRoomError`로 사용자용 문구와 개발자용 상세(console)를 분리. RLS 설정 오류 메시지가 사용자에게 그대로 노출되던 문제 해결.
- RLS 가이드 갱신: 정책 레벨 입력 검증, 24시간 만료 정책, `pg_cron` 정리 작업, DB 레벨 rate limit 트리거 예시.

## Next action item
1. Supabase 실환경에서 2대 이상 접속해 폭탄 돌리기 **동시 PASS/종료 직전 연타** 시 결과가 1회만 기록되는지 검증.
2. `app/coffee`, `app/roulette`는 아직 에러 배너 UI가 없어, 만료된 링크로 직접 들어오면 조용히 로컬 모드로 동작합니다. 두 페이지에도 안내 UI 추가 필요.
3. 방 생성을 서버(Route Handler/Edge Function)로 옮겨 IP 단위 rate limit 적용 검토.
4. 폭탄 돌리기 벌칙(당첨자 미션) 표시 — 기획서 5절 선택 항목.

## 주의사항
- 로그인 없는 익명 협업 방식이므로 **신원 보장은 제공하지 않습니다.** 방 링크를 아는 사람은 누구나 상태를 바꿀 수 있습니다.
- 쿠키 삭제/브라우저 변경 시 actor가 바뀌며, 진행 중이던 방에서는 다른 사람으로 보입니다.
- 클라이언트 rate limit은 localStorage 기반이라 **남용 방지가 아니라 오조작 방지** 수준입니다.
- 별칭(`유저N`)은 참가자 집합이 바뀌면 번호가 다시 매겨질 수 있습니다.

## 사용자가 해야할 일
1. **Supabase에서 `docs/supabase-rls-guide.md`의 SQL 적용** — 특히 2)의 입력 검증 조건과 3)의 만료 정리(`pg_cron`). 앱 코드만으로는 방어되지 않습니다.
2. 2대 이상 기기로 폭탄 돌리기 방을 만들어 준비/시작/PASS/종료 흐름 확인.
3. 존재하지 않는 방 ID, 다른 게임의 방 ID를 넣었을 때 안내 문구가 맞는지 확인.
4. 방 생성 rate limit 기준(10분 5개)이 실제 사용 패턴에 맞는지 확인 후 조정 요청.
