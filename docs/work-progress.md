# 작업 진행상황

## 현재 상태
- 룰렛 Loading 단계에 결과 텍스트(당첨 항목)와 `다시 돌리기` 버튼을 추가해 게임 한 사이클을 완성했습니다.
- 룰렛 비실시간(단독) 모드에서도 결과 인덱스를 생성·유지하도록 로컬 상태(`localResultIndex`)를 추가했습니다.
- 룰렛 실시간 모드에서는 host가 생성한 `resultIndex`를 viewer가 동일하게 사용하도록 유지해 공정한 화면 공유가 가능하도록 구성했습니다.

## Next Action Item
1. Supabase 실환경에서 Host/Viewer 2대 이상 접속으로 룰렛 결과 동기화(동일 당첨 항목 표시) 재검증
2. 룰렛 단계 전환 시 잔상/중복 애니메이션이 없는지 UX 점검
3. Coffee 페이지의 기존 Hook dependency 경고 정리(기능 영향 없음, 코드 품질 개선 목적)

## 주의사항
- 실시간 동기화는 Supabase 환경변수 설정이 되어 있어야만 동작합니다.
- Viewer는 실시간 방에서 입력/단계 변경 권한이 없고 Host 상태를 그대로 반영합니다.
- 기존 room 데이터 스키마와 신규 상태를 혼용하면 예상치 못한 화면 상태가 발생할 수 있습니다.

## 사용자가 해야 할 일
- TOC 화면이 포함된 파일 경로 또는 해당 기능 브랜치 정보를 공유해주세요.
- `.env`에 Supabase 환경변수 설정
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
- 실제 기기 2대 이상으로 룰렛 room 링크 공유 후 결과 일치 여부 확인
- 운영 환경에서 오래된 room 데이터 정리 정책(만료/삭제) 적용 여부 검토
